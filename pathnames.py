import pathlib

predictor_path = pathlib.Path(r'') # path to save generated predictor files
rawdata_path = pathlib.Path(r'') # path to raw EEG data
preprocessed_path = pathlib.Path(r'') # path to save preprocessed EEG data
clicks_path = pathlib.Path(r'') # path to output click ERPs
speech_path = pathlib.Path(r'') # path to output speech TRFs
postanalysis_path = pathlib.Path(r'') # path to save post-analysis plots
